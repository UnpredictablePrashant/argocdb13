apiVersion: v1
kind: Service
metadata:
  name: mongodbb13-service
  namespace: tmb13db-argocd
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
    - protocol: TCP
      port: 27017
      targetPort: 27017